//1 - soma 2 - subtrai 3- sair

principal {
    inteiro op;
    inteiro a, b;
    leia(a);
    leia(b);

    leia(op);
    escolha(op) {
        caso 1:
            somar(a, b);
            pare;
        caso 2:
            subtrair(a, b);
            pare;
        caso 3:
            //sair
            pare;
        casocontrario:
            imprima("Comando invalido");
            pare;
    }
}