funcao fatorial(inteiro numero) -> inteiro {
    se(numero > 1){
        retorne numero * fatorial(numero - 1);
    }
    senao {
        retorne 1;
    }
}


principal() {
    inteiro numero;
    imprima("Fatorial de qual numero?:")
    leia(numero);
    
    imprima(fatorial(numero));
}